# Claude 工程入口

这是一个 Unity 2.5D 本地/在线平台射击派对游戏原型。当前版本为 **Prototype 0.4.0**，工程状态以 `PROJECT_STATE.md` 为唯一事实来源。

## 开始工作前

按顺序阅读：

1. `docs/2026-09-02_工程交接-Claude-0.3.0-report.md`
2. `PROJECT_STATE.md`
3. `NEXT_TASKS.md`
4. `updates/CANDIDATES.md`
5. `updates/VERSION_0_4_0.md`（0.3.0日志保留为已关闭里程碑）
6. `GAME_VISION.md`
7. 仅在需要历史理由时读取 `DECISIONS.md`

`docs/2026-09-02_工程交接-Codex-report.md`是0.2.5架构历史；当前交接以第一项0.3.0报告为准。

不要把 `docs/archive/` 中的历史状态覆盖当前文件。

## 当前工程

- Unity项目：`game-client/`
- Unity版本：`6000.5.10f1`
- URP：`17.5.0`
- 平台：Windows
- 构建：`game-client/Tools/build-prototype.ps1`
- 验证：`game-client/Tools/verify-prototype.ps1`
- 可执行文件：`game-client/Builds/Prototype01/ChaosArenaPrototype.exe`

```powershell
& .\game-client\Tools\verify-prototype.ps1
& .\game-client\Tools\build-prototype.ps1
```

构建前如果游戏正在运行，只结束名称精确为 `ChaosArenaPrototype` 的进程；不要使用破坏性Git或文件清理命令。

## 当前0.4.0能力

- 单机可选择1–3名机器人，Easy/Normal/Hard，默认Easy。
- Relay房间码Host/Join已由两实例真人验证；主机权威同步移动、射击、生命、结算与随机掉落。
- 2.5D六平台竞技场、单向穿台、二段跳、分阶段重力、水平射击、场外扣库存生命。
- 几何战士采用立方体/球体/四面体/圆柱体剪影、发光朝向眼、果冻材质和弹簧颤动。
- 基础手枪/Carbine无限弹药；Pulse SMG、Scatter和Sniper通过随机定时掉落获取。
- 三份第三方素材均为CC0，登记在 `docs/archive/ASSET_POLICY.md`。
- 0.2.6将全部战斗平台仅在X轴放宽10%，并把主机权威出界线放宽到 `|x|>19 / y<-9.5`，给空中控制与二段跳留出回场空间；手感待用户确认。
- 0.2.6将狙击枪基础击退调为 `(7.0,2.4)`、冷却调为1.7秒；伤害10、弹速46和3发弹药不变，手感待用户确认。
- 0.3.0新增本地双人：P1键盘，P2第一只手柄或备用方向键布局，共享双目标镜头，无AI。用户已确认P2移动与A跳跃通过。
- 同一0.3.0里继续完善P2：LT/RT均可射击，并保留X/RB与键盘后备键。射击按武器施加真实反向刚体速度和短暂动量保留。
- 同一0.3.0加入显示设置：720p至4K及显示器原生分辨率、窗口/无边框全屏、设置持久化和高分辨率UI缩放。
- 同一0.3.0加入本地三人：P1键盘、P2第一只Gamepad、P3第二只Gamepad、无AI、三目标镜头和三人屏外提示。Input System 1.19.0与旧输入并存。**三人实体输入已由用户于2026-09-03确认通过**。
- 0.4.0（2026-09-03）：
  - 击中表现分三层：冲击环、沿真实冲量方向的火星扇形、以及随速度连续变化的单条 `TrailRenderer` 光带。离散残影方案已废弃。
  - AI改为五档 EASY/NORMAL/HARD/EXPERT/MASTER，全部数值集中在 `BotProfile.cs`，整条曲线上移一级，默认档位改为NORMAL。
    强度来自预判、视线检测、武器射程纪律、闪避与被击飞自救，不给AI任何数值优势。
  - 界面：竞技场底部常驻文字移入菜单；HUD改为只显示生命的计分卡（真人左上、机器人右上）；主菜单简化为单人/多人；
    新增手柄占座大厅；本地人数上限提升到4人。
  - 武器：冲锋枪/霰弹/狙击模型朝向修正，地面掉落放大，冷却≥0.3秒的武器新增充能读条环。
  - 内容：四张主题地图（NEON CITY / DESERT / STARSHIP / OCEAN）、移动平台、SHIELD与SHRINK两个道具。
  - 本版本全部内容尚未真人试玩，构建与冒烟只证明能跑起来。
  - 自动断言标记为 `CHAOS_ARENA_040_ASSERTIONS_PASS`；玩家版本号 `bundleVersion` 为 `0.4.0`。

## 当前优先级

三人同时输入已确认，0.3.0里程碑关闭。接下来优先完成0.4.0的人工试玩：

- 速度光带阈值9.5、长度与宽度是否合适；
- 五档AI实际强度曲线；被击飞自救是否让击飞收益过低（如是可限制为仅EXPERT/MASTER）；
- 三把枪修正后的朝向，以及手枪是否确实无需修正；
- 充能读条环是否遮挡视线；
- 四张地图的可玩性与移动平台节奏；
- SHIELD/SHRINK的时长与掉落占比；
- 四人本地同乐（需三只实体手柄，尚未实测）；
- LT/RT、四种武器后坐位移、4K/无边框切换及重启后的设置保持；
- 随机掉落5.5–9.5秒节奏与三个槽位上限；
- 狙击枪削弱后的强度和Scatter贴脸强度；
- 黄昏背景、平台、角色与弹丸的可读性；
- 客户端一个往返的输入延迟；
- 四人满员行为（当前只验证到两实例）。

当前没有获批的新玩法批次。U-030客户端预测、U-029等待室、U-028模式框架、U-009抓边及所有新模式均不得擅自开始。

## 重要边界

- 新建议默认写入 `updates/CANDIDATES.md`，攒成连贯版本并获得用户明确批准后再改代码。
- 正式方向现在同时包含同机双人与2–4人在线；本地双人共享镜头，在线每台客户端跟随自己拥有的角色（D-020取代D-002的同机限制）。
- 当前联网技术为Netcode for GameObjects + Unity Relay；Steam网络与平台集成尚未选定。
- 不使用参考Flash游戏的代码、地图、UI、角色、音效或素材制作公开版本。
- 网络素材必须记录商业许可和来源；优先CC0/可商用免费素材。
- 当前美术、音效和数值均为原型，不得宣称生产完成。
- 本地项目文件是Codex与Claude协作时的主要工作副本。每次开始和结束版本迭代时，先检查工作树、`main`、`origin/main`和对应版本标签；如本地与远端不一致，先查看提交和差异并保留未提交内容，不要盲目pull、push、merge或覆盖。

## 回归保护

- 装饰性primitive的碰撞体必须先禁用再销毁，避免弹丸在枪口自毁。
- `FighterMotor`受击控制锁必须保留，避免移动控制覆盖击退。
- URP透明状态使用 `PrototypeJelly.mat` 资源资产，不在运行时代码中猜混合状态。
- 客户端只有在确实连接成功过以后，断开才可被判定为掉线；不能在连接批准期间提前踢回菜单。
- `BotController.SetTarget` 必须在目标未变时提前返回。`RetargetBots()` 每帧调用它，无条件清空瞄准状态会让反应计时器永远累积不到档位所需时长，机器人会走位会闪避但一枪不开。`AssertBotsCanActuallyShoot` 保护这一点，并已做过反向验证。
- 武器枪口朝向以实机渲染为准，不要靠测量OBJ网格推导：导入过程自带手性转换，测量结果与最终朝向并不一一对应。
- 移动平台必须挂运动学刚体，且 `FighterMotor` 必须把脚下平台速度并入目标速度，否则站上去会被直接刮下来。
- 修改这些路径后必须确认相关断言仍有意义，不能只看构建成功。

## 开发流程

用户随时提需求，但不一定立刻实施；默认先记入 `updates/CANDIDATES.md` 攒批，由当前协作助手判断何时能组成连贯、可测试的版本批次。

版本节奏（用户2026-09-03明确）：**不要频繁推版本号**。小修、小维护合并进当前版本日志；
只有成组的大改动才推进版本号。何时推进由当前协作助手自行判断，不必每次询问。

批准规则按改动性质分类（用户2026-09-02确认，细化D-018的"用户批准"）：

| 改动性质 | 是否先问用户 |
|---|---|
| 修bug、调数值、用户已明确提过的需求 | 不用问，做完再报告 |
| 新玩法、改设计方向、助手自己提出的新功能 | 先列清单，等用户点头 |

每个版本批次的固定步骤：

1. 开工前检查 `git status`、当前提交、版本标签和 `origin/main`；不一致时先看差异，禁止盲目覆盖。
2. 小范围改代码。
3. 运行Unity构建和 `-chaosSmokeTest`。
4. 同步更新 `PROJECT_STATE.md`、`NEXT_TASKS.md`、`updates/VERSION_*.md` 和 `updates/CANDIDATES.md`。
5. 提交、打与版本一致的 `v0.x.y` 标签、确认不含密钥后推送。

版本号只在形成新的、连贯且可测试的里程碑时推进。同一里程碑内的bug修复、数值微调、输入修正、界面补全与用户反馈整合只更新该版本日志，不连续增加补丁号。

## 远程仓库

- `https://github.com/GhostGrago/chaos-arena-prototype`（**public**），默认分支 `main`。
- **本地文件是工作事实来源**；GitHub是公开版本记录。开始和结束版本批次时都要比较本地与远端提交。
- Codex与Claude不会同时工作（一方上传时另一方休息），因此不需要处理并发冲突；谁改完谁提交即可。
- 0.1.1–0.1.3从未进入git，无法重建；`v0.1.4` 是可追溯历史的起点。
- public仓库不得提交Unity/Relay/Steam密钥、账号令牌或私有服务配置。

## 修改约定

- 使用小范围、可验证修改，保留用户现有文件。
- 修改后至少运行Unity构建和 `-chaosSmokeTest`。
- 不把编译/烟雾通过等同于主观手感通过。
- 同步更新 `PROJECT_STATE.md`、`NEXT_TASKS.md`、对应 `updates/VERSION_*.md` 和 `SESSION_FINAL_HANDOFF.md`。
